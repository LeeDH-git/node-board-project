<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8" />
  <title>현장 메인 메뉴</title>
  <style>
    * {
      box-sizing: border-box;
    }
    body {
      margin: 0;
      font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
      background: #f5f5f5;
    }
    .container {
      max-width: 600px;
      margin: 60px auto;
      padding: 24px;
      background: #ffffff;
      border-radius: 16px;
      box-shadow: 0 8px 20px rgba(0,0,0,0.08);
    }
    h1 {
      margin-top: 0;
      margin-bottom: 4px;
      font-size: 24px;
      text-align: center;
    }
    .subtitle {
      text-align: center;
      font-size: 13px;
      color: #777;
      margin-bottom: 24px;
    }
    .menu-list {
      display: grid;
      grid-template-columns: 1fr;
      gap: 12px;
    }
    .menu-item {
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 14px 16px;
      border-radius: 10px;
      border: 1px solid #e0e0e0;
      background: #fafafa;
      text-decoration: none;
      color: #222;
      transition: background 0.15s ease, transform 0.1s ease, box-shadow 0.1s ease;
    }
    .menu-item:hover {
      background: #f0f4ff;
      transform: translateY(-1px);
      box-shadow: 0 4px 10px rgba(0,0,0,0.05);
    }
    .menu-title {
      font-size: 16px;
      font-weight: 600;
    }
    .menu-desc {
      font-size: 12px;
      color: #777;
      margin-top: 2px;
    }
    .menu-right {
      font-size: 12px;
      color: #999;
    }
    @media (max-width: 480px) {
      .container {
        margin: 30px 12px;
        padding: 16px;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>현장 관리 시스템</h1>
    <div class="subtitle">메뉴를 선택하세요</div>

    <div class="menu-list">
      <!-- 1. 견적관리 -->
      <a href="/estimate" class="menu-item">
        <div>
          <div class="menu-title">1. 견적관리</div>
          <div class="menu-desc">공사별·거래처별 견적 등록 / 조회</div>
        </div>
        <div class="menu-right">ENTER</div>
      </a>

      <!-- 2. 기성관리 -->
      <a href="/progress" class="menu-item">
        <div>
          <div class="menu-title">2. 기성관리</div>
          <div class="menu-desc">월별 기성 내역 및 정산 관리</div>
        </div>
        <div class="menu-right">ENTER</div>
      </a>

      <!-- 3. 직원관리 -->
      <a href="/staff" class="menu-item">
        <div>
          <div class="menu-title">3. 직원관리</div>
          <div class="menu-desc">직원 정보 / 노임 / 투입 현황 관리</div>
        </div>
        <div class="menu-right">ENTER</div>
      </a>

      <!-- 4. 계약관리 -->
      <a href="/contract" class="menu-item">
        <div>
          <div class="menu-title">4. 계약관리</div>
          <div class="menu-desc">하도급·자재·용역 계약서 관리</div>
        </div>
        <div class="menu-right">ENTER</div>
      </a>
    </div>
  </div>
</body>
</html>
