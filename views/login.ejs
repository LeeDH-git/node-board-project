<div class="auth-wrap">

    <!-- ✅ 로그인 대문 -->
    <div class="login-hero">
        <h1>현장관리 시스템</h1>
        <p>로그인 후 홈 화면으로 이동합니다.</p>
    </div>

    <div class="auth-card">
        <div class="auth-title">로그인</div>

        <% if (error) { %>
            <div class="auth-error">
                <%= error %>
            </div>
            <% } %>

                <form method="post" action="/login" class="auth-form">
                    <label class="auth-label">아이디</label>
                    <input name="username" class="auth-input" autocomplete="username" required />

                    <label class="auth-label">비밀번호</label>
                    <input name="password" type="password" class="auth-input" autocomplete="current-password"
                        required />

                    <button type="submit" class="auth-btn">로그인</button>
                </form>              
    </div>

</div>