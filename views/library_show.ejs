<link rel="stylesheet" href="/css/style.css">

<main id="library_show">
<div class="page-head">
  <div>
    <div class="page-title">
      <%= doc.title || '자료 상세' %>
    </div>
    <!-- ✅ 사용자가 원한대로: "자료 정보 및 다운로드" 아래에 버튼이 없어야 하므로,
         여기에는 문구만(또는 필요 없으면 이 div 자체를 제거) -->
    <!-- <div class="page-sub">
      자료 정보 및 다운로드
    </div> -->
  </div>

</div>


  <!-- <div class="c-wrap"> -->

    <div class="c-card">
      <div class="c-title">기본 정보</div>

      <div class="c-meta">
        <div>
          <div class="c-kv">
            <div class="c-k">제목</div>
            <div class="c-v">
              <%= doc.title || '-' %>
            </div>
          </div>

          <div class="c-kv">
            <div class="c-k">구분</div>
            <div class="c-v">
              <% if (doc.doc_type==='form' ) { %> 서식
                <% } else if (doc.doc_type==='reference' ) { %> 자료
                  <% } else if (doc.doc_type==='safety' ) { %> 안전
                    <% } else if (doc.doc_type==='contract' ) { %> 계약
                      <% } else { %> - <% } %>
            </div>
          </div>
        </div>

        <div>
          <div class="c-kv">
            <div class="c-k">카테고리</div>
            <div class="c-v">
              <%= doc.category || '-' %>
            </div>
          </div>

          <div class="c-kv">
            <div class="c-k">등록일</div>
            <div class="c-v">
              <%= doc.created_at ? String(doc.created_at).substring(0,10) : "-" %>
            </div>
          </div>
        </div>
      </div>
    </div>

    <div class="c-card">
      <div class="c-section-title">첨부 파일</div>

      <div class="c-kv" style="border-bottom: none;">
        <div class="c-k">다운로드</div>
        <div class="c-v">
          <% if (doc && doc.filename) { %>
            <a class="c-link" href="/uploads/library/<%= doc.filename %>" target="_blank">
              <%= doc.original_name || doc.filename %>
            </a>
            <span style="color: var(--muted); margin-left: 6px; font-size: 12.5px;">
              (<%= doc.size ? Math.round(doc.size/1024) + ' KB' : '0 KB' %>)
            </span>
            <% } else { %>
              <span style="color: var(--muted);">-</span>
              <% } %>
        </div>
      </div>
    </div>

    <div class="c-card">
      <div class="c-section-title">메모</div>
      <div class="c-kv" style="border-bottom: none; display: block;">
        <div style="font-size: 13.5px; color: var(--text); line-height: 1.7; white-space: pre-wrap;">
          <%= doc.memo || '-' %>
        </div>
      </div>
    </div>
<!-- 
  </div> -->
</main>