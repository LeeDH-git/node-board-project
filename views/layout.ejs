<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1"/>
  <title><%= (typeof title !== 'undefined' ? title : "현장 관리 시스템") %></title>
  <style>
    :root{
      --bg: #F9FAFB;
      --panel: #FFFFFF;
      --text: #111827;
      --muted: #6B7280;
      --border: #E5E7EB;
      --hover: #F3F4F6;
      --primary: #111827;
      --primaryHover: #0B1220;
      --radius: 12px;
      --shadow: 0 1px 2px rgba(0,0,0,.05);
      --ring: 0 0 0 4px rgba(17,24,39,.14);
    }

    * { box-sizing: border-box; }
    body{
      margin: 0;
      background: var(--bg);
      color: var(--text);
      font-family: Inter, system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif;
      font-size: 14.5px;
      line-height: 1.6;
    }
    a{ color: inherit; text-decoration: none; }

    .app{
      min-height: 100vh;
      display: flex;
    }

    /* Sidebar */
    .sidebar{
      width: 256px;
      background: var(--panel);
      border-right: 1px solid var(--border);
      padding: 16px;
      position: sticky;
      top: 0;
      height: 100vh;
      display: flex;
      flex-direction: column;
      gap: 16px;
    }
    .brand{
      display: flex;
      align-items: center;
      gap: 10px;
      padding: 8px 10px;
      border-radius: 10px;
    }
    .brand-badge{
      width: 28px; height: 28px;
      border-radius: 8px;
      background: var(--text);
      color: #fff;
      font-weight: 700;
      display: grid;
      place-items: center;
      font-size: 12px;
      letter-spacing: .5px;
    }
    .brand-title{
      font-weight: 700;
      font-size: 14px;
    }
    .nav-title{
      font-size: 12px;
      color: var(--muted);
      margin: 6px 10px;
      text-transform: uppercase;
      letter-spacing: .06em;
    }
    .nav{
      display: flex;
      flex-direction: column;
      gap: 4px;
    }
    .nav a{
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 9px 10px;
      border-radius: 10px;
      color: var(--text);
    }
    .nav a:hover{
      background: var(--hover);
    }
    .nav a.active{
      background: var(--hover);
      font-weight: 600;
    }
    .nav small{
      color: var(--muted);
      font-size: 12px;
    }

    /* Main */
    .main{
      flex: 1;
      padding: 24px 28px;
    }
    .topbar{
      display: flex;
      align-items: center;
      justify-content: space-between;
      margin-bottom: 16px;
    }
    .page-title{
      font-size: 22px;
      font-weight: 650;
      letter-spacing: -0.01em;
      margin: 0;
    }
    .page-sub{
      margin: 2px 0 0;
      color: var(--muted);
      font-size: 13px;
    }

    .btn{
      height: 36px;
      padding: 0 14px;
      border-radius: 10px;
      border: 1px solid var(--border);
      background: var(--panel);
      color: var(--text);
      cursor: pointer;
    }
    .btn:hover{ background: var(--bg); }
    .btn-primary{
      border-color: transparent;
      background: var(--primary);
      color: #fff;
    }
    .btn-primary:hover{ background: var(--primaryHover); }

    .panel{
      background: var(--panel);
      border: 1px solid var(--border);
      border-radius: var(--radius);
      box-shadow: var(--shadow);
      padding: 18px;
    }

    @media (max-width: 900px){
      .sidebar{ display:none; }
      .main{ padding: 18px 14px; }
    }
  </style>
</head>

<body>
  <div class="app">
    <aside class="sidebar">
      <div class="brand">
        <div class="brand-badge">MC</div>
        <div>
          <div class="brand-title">현장관리 시스템</div>
          <div style="font-size:12px; color:var(--muted);">Console</div>
        </div>
      </div>

      <div>
        <div class="nav-title">Manage</div>
        <nav class="nav">
          <a href="/" class="<%= (typeof active !== 'undefined' && active === 'home') ? 'active' : '' %>">
            <span>Home</span><small>메인</small>
          </a>
          <a href="/estimate" class="<%= (typeof active !== 'undefined' && active === 'estimate') ? 'active' : '' %>">
            <span>견적관리</span><small>/estimate</small>
          </a>
          <a href="/progress" class="<%= (typeof active !== 'undefined' && active === 'progress') ? 'active' : '' %>">
            <span>기성관리</span><small>/progress</small>
          </a>
          <a href="/staff" class="<%= (typeof active !== 'undefined' && active === 'staff') ? 'active' : '' %>">
            <span>직원관리</span><small>/staff</small>
          </a>
          <a href="/contract" class="<%= (typeof active !== 'undefined' && active === 'contract') ? 'active' : '' %>">
            <span>계약관리</span><small>/contract</small>
          </a>
          <a href="/library" class="<%= (typeof active !== 'undefined' && active === 'library') ? 'active' : '' %>">
            <span>자료관리</span><small>/library</small>
          </a>
        </nav>
      </div>

      <!-- <div style="margin-top:auto; padding:10px; border:1px solid var(--border); border-radius:12px; background:var(--bg);">
        <div style="font-weight:600; font-size:13px;">Tip</div>
        <div style="color:var(--muted); font-size:12px; margin-top:4px;">
          공통 레이아웃을 쓰면 화면 통일·유지보수가 쉬워집니다.
        </div>
      </div> -->
    </aside>

    <main class="main">
      <div class="topbar">
        <div>
         <h1 class="page-title">
            <%= (typeof headerTitle !== 'undefined' ? headerTitle : "현장 관리 시스템") %>
         </h1>

         <% if (typeof headerSub !== 'undefined' && headerSub) { %>
            <p class="page-sub"><%= headerSub %></p>
         <% } %>
        </div>

        <% if (typeof headerAction !== 'undefined' && headerAction) { %>
            <div><%- headerAction %></div>
        <% } %>
      </div>

      <div class="panel">
        <%- body %>
      </div>
    </main>
  </div>
</body>
</html>
